{% extends "TiendaBundle::base.html.twig" %}

{% block body %}
    
<form action="{{ path('tienda_usuarios_editar', { 'id' : usuario.id }) }}" method="POST" class="ligthform">
    <fieldset>
        <legend>Actualizar Usuario</legend>
        <input type="hidden" name="id" value="{{ usuario.id }}"/>
        <label>Username</label><input type="text" name="username" maxlength="20" class="form-control" value="{{ usuario.username }}"/><br/>
        <label>Password</label><input type="password" name="password" maxlength="40" class="form-control" value="{{ usuario.password }}"/><br/>
        <label>Nombres</label><input type="text" name="nombres" maxlength="100" class="form-control" value="{{ usuario.nombres }}"/><br/>
        <label>Email</label><input type="email" name="email" maxlength="100" class="form-control" value="{{ usuario.email }}"/><br/>
        <label>Rol</label><select name="roles_id" class="form-control">
            {% for rol in roles %}
                <option value="{{ rol.id }}" {% if rol.id == usuario.rol.id %} selected="selected" {% endif %}>{{ rol.nombre }}</option>
            {% endfor %}
        </select><br/>
        <input type="submit" value="Guardar"/>
    </fieldset>
</form>

<br/>

<a href="{{ path('tienda_usuarios_index') }}" class="btn btn-default">&lt;&lt; Regresar</a>

{% endblock %}

